// Archivo de configuración para el frontend
// Puedes definir aquí la URL base de la API para producción o desarrollo
// Ejemplo de uso: window.config.baseURL

window.config = {
  // Cambia esta URL por la de tu servidor real en producción
  baseURL: '' // Ejemplo: 'https://midominio.com/api' o dejar vacío para rutas relativas
}; 